apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-persistence-test
  namespace: testing
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
